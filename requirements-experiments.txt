torch>=2.0
torchvision>=0.15
numpy>=1.24
reportlab>=4.0          # paper/build_paper.py (regenerate the PDF)
datasets>=2.0           # experiments/longmemeval.py (stream LongMemEval)
# optional, for real semantic retrieval in the memory demo/benchmark:
sentence-transformers>=3.0   # recommended for longmemeval.py --split s
